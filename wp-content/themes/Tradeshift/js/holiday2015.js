// Delivery Services JS only
// --------------------------------------------------------------------


// Your success story starts here
// ------------------------------------------

$(document).ready(function($){
  // if (!Modernizr.svg) {
  //   $("object .santa").attr("data", "../img/holiday2015/santa-hero.png");
  // };

  $('#snowfall').snowfall ({
      flakeCount : 60,
      maxSpeed : .5,
      maxSize : 5,
      round: true
  });
});

